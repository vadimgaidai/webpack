mixin svg-icon(name,mod)
	- mod = mod || ''
	svg(class="icon icon-" + name + ' ' + mod)
		use(xlink:href="images/symbol/sprite-icon.svg#" + name)

mixin svg-img(name,mod)
	- mod = mod || ''
	svg(class="icon icon-" + name + ' ' + mod)
		use(xlink:href="images/symbol/sprite-images.svg#" + name)